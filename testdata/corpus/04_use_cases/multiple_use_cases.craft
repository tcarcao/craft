use_case "Login" {
    when User submits credentials
        AuthService validates credentials
        AuthService notifies "User Logged In"
}

use_case "Logout" {
    when User requests logout
        AuthService invalidates session
        AuthService notifies "User Logged Out"
}
