version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: craft
    main: ./cmd/craft
    binary: craft
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

archives:
  - id: craft
    name_template: "craft_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^test:"

brews:
  - name: craft
    ids: [craft]
    repository:
      owner: tcarcao
      name: homebrew-craft
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/tcarcao/craft"
    description: "DDD modeling CLI with language server — parse, lint, inspect, visualize, and edit .craft files"
    license: "MIT"

  - name: "craft@{{ .Major }}.{{ .Minor }}"
    ids: [craft]
    repository:
      owner: tcarcao
      name: homebrew-craft
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/tcarcao/craft"
    description: "DDD modeling CLI with language server — parse, lint, inspect, visualize, and edit .craft files"
    license: "MIT"
